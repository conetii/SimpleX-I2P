# Database encryption password (generated by scripts/setup.sh)
SMP_DB_PASSWORD=change_me_to_random_password

# Server listen port
SMP_PORT=5223

# Database path (inside container)
SMP_DB_PATH=/data/smp.db

# TLS certificate paths (auto-generated if missing)
SMP_TLS_CERT=/data/server.crt
SMP_TLS_KEY=/data/server.key
